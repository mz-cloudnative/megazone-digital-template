server:
  port: 8080
spring:
  profiles:
    active:
      - dev
    group:
      local:
        - db-local
        - kafka-local
        - cache-local
      dev:
        - db-dev
        - kafka-dev
        - cache-dev
    include:
      - db
      - kafka
      - cache