server:
  port: 8080


spring:
  application:
    name: spring-boot-backend

  datasource:
    master:
      hikari:
        driver-class-name: org.postgresql.Driver
        jdbc-url: jdbc:postgresql://localhost:5432
        username: username
        password: password
    slave:
      hikari:
        driver-class-name: org.postgresql.Driver
        jdbc-url: jdbc:postgresql://localhost:5433
        username: username
        password: password

  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        order_updates: true
        order_inserts: true
        jdbc:
          batch_size: 100

  flyway:
    locations: classpath:db/migration,classpath:db/seed
    enabled: true
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5432/
    user: username
    password: password
    #out-of-order: true
    validate-on-migrate: true
    validate-migration-naming: true

  redis:
    host: localhost
    port: 6379

  cache:
    type: redis
    redis:
      time-to-live: 60000 #60초
      expire-time:
        ttl:
          staff: 60
          player: 120

  kafka:
    bootstrap-servers: kafka-1:19092, kafka-29092, kafka:39092
    consumer:
      group-id: group-1
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all

  rest:
    connect-time: 5000 # 5초
    read-time: 5000 # 5초
    write-time: 5000 # 5초


mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

info:
  external:
    url: "https://api-football-v1.p.rapidapi.com/v3/teams/countries"
    headerKey:
      key: "X-RapidAPI-Key"
      host: "X-RapidAPI-Host"
    headerValue:
      key: "1cbf2f38a4msh07b8e15a1dcb899p11faa5jsnabfd355d9b81"
      host: "api-football-v1.p.rapidapi.com"